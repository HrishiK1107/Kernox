# Kernox Frontend

SOC dashboard UI for the Kernox detection platform.

Built with:

- React
- Vite
- TailwindCSS
- Recharts

The interface visualizes alerts, endpoint risk, and detection analytics.

Run locally:

cd frontend 
npm install
npm run dev